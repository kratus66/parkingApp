// TEMPORARILY DISABLED - WebSocket functionality
// This module will be re-enabled once Socket.IO types are properly configured

// Stub export to prevent import errors
export class RealtimeGateway {
  emitSpotUpdated() {}
  emitOccupancyUpdated() {}
  emitSpotStatusChanged() {}
  emitTicketCreated() {}
  emitTicketClosed() {}
}
